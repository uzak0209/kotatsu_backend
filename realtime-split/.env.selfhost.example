# Public hostname resolved by DDNS
PUBLIC_HOSTNAME=kotatsu.ruxel.net

# Exposed ports (router forwards these to this host)
API_PORT=8080
QUIC_PORT=4433

# Internal-only gRPC control plane port (not exposed on router)
GRPC_PORT=50051
